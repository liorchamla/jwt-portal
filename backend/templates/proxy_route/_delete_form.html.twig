<form method="post" action="{{ path('app_proxy_route_delete', {'id': proxy_route.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proxy_route.id) }}">
    <button class="btn">Delete</button>
</form>
